org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.warning.mode=all

# Point Gradle's toolchain detection at your Oracle GraalVM (JDK 25) install,
# either by uncommenting this with your own path or by exporting GRAALVM_HOME.
# org.gradle.java.installations.paths=/path/to/oracle-graalvm-25.2.4
